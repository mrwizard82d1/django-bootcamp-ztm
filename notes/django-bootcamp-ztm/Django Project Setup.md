Typical set up seems to use `venv` for virtual environment

Create the typical project files
- `django-admin startproject <project-name> .`

Run the server
- `python manage.py runserver`
- Or use "run" button of PyCharm

